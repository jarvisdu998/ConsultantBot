# 课程管理系统后端设计总结

## 项目概述
基于Spring Boot + MyBatis + MySQL的课程管理系统后端，实现了学员和管理员的完整功能模块。

## 技术栈
- **框架**: Spring Boot 3.5.0
- **数据库**: MySQL 8.0
- **ORM**: MyBatis
- **缓存**: Redis
- **工具**: Lombok
- **AI集成**: LangChain4j

## 数据库设计

### 核心表结构
1. **student（学员表）** - 存储学员基本信息
2. **admin（管理员表）** - 存储管理员信息
3. **course（课程表）** - 存储课程信息
4. **orders（订单表）** - 存储订单信息
5. **reservation（预约表）** - 存储预约信息

## 功能模块

### 学员功能
- ✅ **登录注册**: 学员可以注册账号并登录系统
- ✅ **预约管理**: 学员可以预约课程、查看预约、取消预约
- ✅ **课程购买**: 学员可以浏览课程、购买课程
- ✅ **订单管理**: 学员可以查看订单、取消订单

### 管理员功能
- ✅ **登录管理**: 管理员登录系统
- ✅ **学员管理**: 对学员进行CRUD操作
- ✅ **课程管理**: 对课程进行CRUD操作，包括上架/下架
- ✅ **订单管理**: 查看和管理所有订单
- ✅ **预约管理**: 查看和管理所有预约
- ✅ **管理员管理**: 管理其他管理员账号

## 项目结构

```
src/main/java/com/consultantbot/consultant/
├── common/
│   └── Result.java                    # 统一响应结果类
├── controller/                        # 控制器层
│   ├── StudentController.java         # 学员控制器
│   ├── AdminController.java           # 管理员控制器
│   ├── CourseController.java          # 课程控制器
│   ├── OrderController.java           # 订单控制器
│   ├── ReservationController.java     # 预约控制器
│   └── AdminStudentController.java    # 管理员管理学员控制器
├── service/                          # 服务层
│   ├── StudentService.java           # 学员服务
│   ├── AdminService.java             # 管理员服务
│   ├── CourseService.java            # 课程服务
│   ├── OrderService.java             # 订单服务
│   └── ReservationService.java       # 预约服务
├── mapper/                           # 数据访问层
│   ├── StudentMapper.java            # 学员数据访问
│   ├── AdminMapper.java              # 管理员数据访问
│   ├── CourseMapper.java             # 课程数据访问
│   ├── OrderMapper.java              # 订单数据访问
│   └── ReservationMapper.java        # 预约数据访问
└── pojo/                            # 实体类
    ├── Student.java                  # 学员实体
    ├── Admin.java                    # 管理员实体
    ├── Course.java                   # 课程实体
    ├── Order.java                    # 订单实体
    └── Reservation.java              # 预约实体
```

## API接口设计

### 学员端接口
- `POST /api/student/login` - 学员登录
- `POST /api/student/register` - 学员注册
- `GET /api/student/{id}` - 获取学员信息
- `PUT /api/student/{id}` - 更新学员信息

### 管理员端接口
- `POST /api/admin/login` - 管理员登录
- `GET /api/admin` - 获取所有管理员
- `POST /api/admin` - 添加管理员
- `PUT /api/admin/{id}` - 更新管理员
- `DELETE /api/admin/{id}` - 删除管理员

### 课程管理接口
- `GET /api/course/active` - 获取上架课程（学员端）
- `GET /api/course` - 获取所有课程（管理员端）
- `POST /api/course` - 添加课程
- `PUT /api/course/{id}` - 更新课程
- `DELETE /api/course/{id}` - 删除课程
- `PUT /api/course/{id}/status` - 更新课程状态

### 订单管理接口
- `GET /api/order/student/{studentId}` - 获取学员订单
- `GET /api/order` - 获取所有订单
- `POST /api/order` - 创建订单
- `DELETE /api/order/{id}` - 取消订单

### 预约管理接口
- `POST /api/reservation` - 添加预约
- `GET /api/reservation/phone/{phone}` - 根据手机号查询预约
- `GET /api/reservation` - 获取所有预约
- `PUT /api/reservation/{id}` - 更新预约
- `DELETE /api/reservation/{id}` - 取消预约

### 管理员管理学员接口
- `GET /api/admin/student` - 获取所有学员
- `POST /api/admin/student` - 添加学员
- `PUT /api/admin/student/{id}` - 更新学员
- `DELETE /api/admin/student/{id}` - 删除学员
- `PUT /api/admin/student/{id}/status` - 更新学员状态

## 安全特性
- **密码加密**: 使用MD5加密存储用户密码
- **数据验证**: 在服务层进行数据有效性验证
- **状态管理**: 支持学员和课程的状态管理（启用/禁用）

## 数据库特性
- **外键约束**: 订单表与学员表、课程表建立外键关系
- **唯一约束**: 用户名、手机号等字段设置唯一约束
- **时间戳**: 自动记录创建时间和更新时间

## 部署说明

### 环境要求
- JDK 17+
- MySQL 8.0+
- Redis 6.0+
- Maven 3.6+

### 数据库初始化
1. 执行 `src/main/db.sql` 创建数据库和表结构
2. 执行 `src/main/resources/init-data.sql` 插入初始数据

### 配置修改
修改 `src/main/resources/application.yml` 中的数据库连接信息：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/course_system
    username: root
    password: 123456
```

### 启动应用
```bash
mvn spring-boot:run
```

## 测试账号
- **管理员**: admin/123456, manager/123456
- **学员**: student001/123456, student002/123456, student003/123456

## 项目亮点
1. **完整的CRUD操作**: 所有实体都支持完整的增删改查操作
2. **分层架构**: 采用标准的MVC分层架构，代码结构清晰
3. **统一响应格式**: 使用统一的响应格式，便于前端处理
4. **数据验证**: 在服务层进行业务逻辑验证
5. **状态管理**: 支持学员和课程的状态管理
6. **API文档**: 提供完整的API接口文档

## 扩展建议
1. **权限管理**: 可以添加基于角色的权限控制
2. **日志记录**: 添加操作日志记录功能
3. **缓存优化**: 对热点数据进行Redis缓存
4. **分页查询**: 对列表查询添加分页功能
5. **文件上传**: 支持课程图片等文件上传功能
6. **消息通知**: 添加订单状态变更等消息通知功能 